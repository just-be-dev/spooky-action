{
  "name": "mouth-ring",
  "source": "face",
  "metrics": {
    "open": "dist(upper_lip, lower_lip) / dist(forehead, chin)",
    "pos": { "expr": "nose_tip", "smooth": 0.5 }
  },
  "initial": "idle",
  "states": {
    "idle": {
      "on": [{ "if": "open > 0.08", "goto": "showing" }]
    },
    "showing": {
      "on": [{ "if": "open < 0.05", "goto": "idle" }]
    }
  }
}
